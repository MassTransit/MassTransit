branches:
  only:
    - develop
    - master
only_commits:
  files:
    - docs/*
    - '**/*.md'
    - '**/*.html'
    - docs.yml
install:
  - npm install
build_script:
  - "%CMD_IN_ENV% npm run docs:publish"