branches:
  only:
    - develop
    - master
only_commits:
  files:
    - docs/*
    - '**/*.md'
    - '**/*.html'
    - docs.yml
install:
  - npm install --no-optional
  - npm install -g gitbook-cli
build_script:
  - "%CMD_IN_ENV% npm run docs:publish"