branches:
  only:
    - develop
    - master
only_commits:
  files:
    - docs/*
    - '**/*.html'
install:
  - ps: Install-Product node 0
  - npm install
build_script:
  - "%CMD_IN_ENV% npm run docs:publish"