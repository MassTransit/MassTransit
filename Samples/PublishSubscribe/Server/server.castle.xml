<?xml version="1.0" encoding="utf-8" ?>
<castle>
	<components>

		<!-- load all of the services here : Windsor.Register(AllTypes.Of(IHostedService)-->
		<!-- use the startable facitlity -->
		<component id="mySimpleServer"
				   lifestyle="singleton"
				   service="MassTransit.IHostedService, MassTransit"
				   type="Server.PasswordUpdateService, Server">
		</component>
	</components>

	<facilities>
		<facility id="masstransit">

			<bus id="main"
				 endpoint="msmq://localhost/mt_server">
				<subscriptionService endpoint="msmq://localhost/mt_pubsub" />
			</bus>

			<transports>
				<transport>MassTransit.Transports.Msmq.MsmqEndpoint, MassTransit.Transports.Msmq</transport>
			</transports>
		</facility>

	</facilities>
</castle>
