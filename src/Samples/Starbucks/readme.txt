I checked in the binaries for the version this was built on, so that it won't be broken with the latest changes.

Next step is to remove the lib folder & create project references to the live MT stuff.